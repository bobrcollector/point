DATABASE_URL=postgresql+asyncpg://point:point@127.0.0.1:5433/point
CORS_ORIGINS=http://localhost:5173
JWT_SECRET=change-me-in-production-point-diploma
APP_PUBLIC_URL=http://localhost:5173
# Публичный URL API для ссылок на загруженные фото (другой ПК в сети: http://192.168.x.x:8000)
API_PUBLIC_URL=http://127.0.0.1:8000
# Web Push (VAPID): npm run generate:vapid
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CLAIMS_EMAIL=mailto:admin@point.local
# Production: разрешить админ-API с публичного домена (через nginx + JWT admin).
# ADMIN_ALLOW_REMOTE=true

